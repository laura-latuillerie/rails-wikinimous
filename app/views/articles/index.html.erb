<% @articles.each do |article| %>
  <article>
    <h3 class="text-primary"><%= link_to article.title, article_path(article) %></h3>
    <p><%= article.content.truncate(200) %></p>
    </article>
<% end %>